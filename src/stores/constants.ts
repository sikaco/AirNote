enum NoteType {HTML, MARK_DOWN}
enum ChapterType {CHAPTER, GROUP}
enum SyncState {DONE, DOING, FAILED}

const Color = {
  BLUE: 'blue',
  RED: 'red',
  BLACK: 'black',
}

export {
  NoteType,
  ChapterType,
  Color,
  SyncState
}